import { Box, Typography } from "@mui/material"

export const ConfigInitState = () => {
    return (
        <Box>
            <Typography variant="h1">ConfigInitState page place holder</Typography>
        </Box>
    )
}