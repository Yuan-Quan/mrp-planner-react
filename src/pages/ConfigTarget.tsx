import { Box, Typography } from "@mui/material"

export const ConfigTarget = () => {
    return (
        <Box>
            <Typography variant="h1">ConfigTarget page place holder</Typography>
        </Box>
    )
}